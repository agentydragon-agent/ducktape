load("@rules_python//python:defs.bzl", "py_library")

exports_files(["pyproject.toml"])
py_library(
    name = "agent_pkg_host",
    srcs = glob(["src/agent_pkg_host/**/*.py"]),
    imports = ["src"],
    visibility = ["//visibility:public"],
    deps = [
        "//mcp_infra",
        "@pypi//aiodocker",
        "@pypi//fastmcp",
    ],
)
