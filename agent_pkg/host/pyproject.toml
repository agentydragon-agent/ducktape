[project]
name = "agent-pkg-host"
version = "0.1.0"
description = "Host-side agent package infrastructure: image building, init runner"
requires-python = ">=3.12"
dependencies = [
    "aiodocker>=0.23",
    "fastmcp>=2.0",
]
