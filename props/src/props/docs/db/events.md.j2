# Events Table

The `events` table stores agent execution traces for analysis.

## events

{{ run_command('psql -c "\\d+ events"') }}

Use this table to analyze how critic runs proceeded, identify patterns in tool usage, and debug failures.
