load("@npm_props_frontend//:defs.bzl", "npm_link_all_packages")

# Link npm packages from pnpm-lock.yaml
npm_link_all_packages(name = "node_modules")

# Export files needed by npm_translate_lock
exports_files([
    "pnpm-lock.yaml",
    "package.json",
])

# NOTE: js_binary for eslint conflicts with node_modules artifact paths
# See bazelization/STATUS.md "JS/TS Linting via Bazel Aspects" section
# For now, use: npm run lint (in props/frontend directory)
