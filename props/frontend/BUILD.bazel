load("@npm_props_frontend//:defs.bzl", "npm_link_all_packages")

# Link npm packages from pnpm-lock.yaml
npm_link_all_packages(name = "node_modules")

# Export files needed by npm_translate_lock
exports_files([
    "pnpm-lock.yaml",
    "package.json",
])

# TODO: js_run_binary for Vite/SvelteKit build not working - artifact prefix conflict with node_modules
# Build with: npm run build (in props/frontend directory)
# Type check with: npm run check
# Lint with: npm run lint
