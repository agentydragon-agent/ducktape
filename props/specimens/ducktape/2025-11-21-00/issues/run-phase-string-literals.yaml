rationale: |
  `run_phase` is assigned using string literals ("idle", "waiting_approval",
  "sampling") instead of a proper enum. Inconsistent with other status fields
  (AgentMode, ServerStatus, ProposalStatus, etc. all use enums).

  Define a `RunPhase` StrEnum and use it throughout.
should_flag: true
occurrences:
  - occurrence_id: occ-0
    files:
      adgn/src/adgn/agent/mcp_bridge/servers/agents.py:
        - - 284
          - 294
        - - 284
          - 284
        - - 292
          - 292
        - - 294
          - 294
    critic_scopes_expected_to_recall:
      - - adgn/src/adgn/agent/mcp_bridge/servers/agents.py
    match_file_restriction:
      - adgn/src/adgn/agent/mcp_bridge/servers/agents.py
