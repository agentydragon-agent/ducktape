rationale: |
  Docker exec MCP tool inputs are extracted manually from dicts with ad-hoc
  coercions (truthy-string checks for bools, string-to-float for timeout, etc.).
  Use a Pydantic model for the tool input so validation is handled by the
  framework and the JSON Schema is auto-exported to MCP clients.
should_flag: true
occurrences:
  - occurrence_id: occ-0
    files:
      llm/adgn_llm/src/adgn_llm/mcp/docker_exec/server.py:
        - - 200
          - 250
        - - 88
          - 112
    critic_scopes_expected_to_recall:
      - - llm/adgn_llm/src/adgn_llm/mcp/docker_exec/server.py
    match_file_restriction:
      - llm/adgn_llm/src/adgn_llm/mcp/docker_exec/server.py
