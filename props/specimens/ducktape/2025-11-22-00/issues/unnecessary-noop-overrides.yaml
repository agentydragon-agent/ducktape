rationale: |
  `NotificationsHandler(BaseHandler)` (lines 244-265) overrides 7 event methods
  that all just `return None`. Base class already provides these no-op defaults.
  Delete the redundant overrides; keep only `__init__` and `on_before_sample`
  which have actual implementation.
should_flag: true
occurrences:
  - occurrence_id: occ-0
    files:
      adgn/src/adgn/agent/reducer.py:
        - [244, 265]
    critic_scopes_expected_to_recall:
      - - adgn/src/adgn/agent/reducer.py
    match_file_restriction:
      - adgn/src/adgn/agent/reducer.py
