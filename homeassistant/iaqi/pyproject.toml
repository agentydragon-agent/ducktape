[project]
name = "indoor_aqi"
version = "0.1.0"
description = "Indoor Air Quality Index component for Home Assistant"
readme = "README.md"
requires-python = ">=3.13"
license = {text = "MIT"}
dependencies = [
    "homeassistant>=2024.12.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-homeassistant-custom-component>=0.13.300",
    "PyHamcrest>=2.1.0",
]
