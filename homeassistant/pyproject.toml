[project]
name = "homeassistant_integrations"
version = "0.1.0"
description = "Home Assistant custom integrations (shared deps)"
requires-python = ">=3.13"
license = {text = "MIT"}
dependencies = [
    "homeassistant>=2025.1.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-homeassistant-custom-component>=0.13.300",
    "PyHamcrest>=2.1.0",
]
