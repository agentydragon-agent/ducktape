test-fixtures/test-trivial:
  source:
    vcs: local
    root: code
  split: train
  bundle: null

test-fixtures/test-validation:
  source:
    vcs: local
    root: code
  split: valid
  bundle: null

test-fixtures/test-validation-2:
  source:
    vcs: local
    root: code
  split: valid
  bundle: null

test-fixtures/test-split-test:
  source:
    vcs: local
    root: code
  split: test
  bundle: null
