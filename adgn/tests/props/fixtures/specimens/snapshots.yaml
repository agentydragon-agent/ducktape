test-fixtures/test-trivial:
  source:
    vcs: local
    root: code
  split: train
  bundle: null

test-fixtures/test-validation:
  source:
    vcs: local
    root: code
  split: valid
  bundle: null
