# Inherit workspace venv from parent
source_up

# Load devenv's direnv integration (provides caching + helpers)
eval "$(devenv direnvrc)"

# Trigger rebuilds when local devenv changes
watch_file devenv.nix

# Activate the adgn-specific devenv (scripts, vite process)
use devenv

# Default registry endpoint for local pushes
export RSPCACHE_REGISTRY_HOST="registry.k3s.local"
