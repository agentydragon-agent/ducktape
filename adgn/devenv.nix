{
  pkgs,
  lib,
  config,
  inputs,
  ...
}: {
  # Python/uv managed by root devenv.nix - this file only handles adgn-specific infra
  # No additional packages needed - Node/UI moved to agent_server
}
