[tool.ruff.lint]
# Ignore specific rules for experimental code
# PLR0913 handled globally in root ruff.toml

[tool.mypy]
# Ignore missing type stubs for experimental code
ignore_missing_imports = true
