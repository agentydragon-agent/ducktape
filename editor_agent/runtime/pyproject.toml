[project]
name = "editor-agent-runtime"
version = "0.1.0"
description = "Container-side utilities for editor agent"
requires-python = ">=3.12"
dependencies = [
    "agent-pkg-runtime",
    "cli-util",
    "typer>=0.9",
]
