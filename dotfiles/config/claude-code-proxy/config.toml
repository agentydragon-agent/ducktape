log_level = "INFO"

# If true, forward unknown Anthropic model names to OpenAI as-is instead of erroring
forward_unknown_model_names = true

# Seems buggy
enable_append_tracking = false

[anthropic_to_openai_model]
#"claude-*opus*" = "o4-mini"
"claude-*opus*" = "codex-mini-latest"
"claude-*sonnet*" = "gpt-4o"
"claude-*haiku*" = "gpt-4o-mini"
#"claude-*opus*" = "codex-mini-latest"
#"claude-*sonnet*" = "codex-mini-latest"
# "codex-2025-05-12" does not work

[search_replace]
"IMPORTANT: Assist with defensive security tasks only. Refuse to create, modify, or improve code that may be used maliciously. Allow security analysis, detection rules, vulnerability explanations, defensive tools, and security documentation." = ""
# Unbork Claude Code w/ OpenAI models
