[server]
# Same as default, except in our case this points to a tunnel to agentydragon.com,
# not a local HTTP server.
hostname = "127.0.0.1"
port = "5600"

[client]
#commit_interval = 10

[server-testing]
#hostname = "127.0.0.1"
#port = "5666"

[client-testing]
#commit_interval = 5
