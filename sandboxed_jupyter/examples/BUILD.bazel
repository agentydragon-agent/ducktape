# gazelle:ignore
load("@rules_python//python:defs.bzl", "py_test")

py_test(
    name = "test_workflow",
    srcs = ["test_workflow.py"],
    deps = [
        "//mcp_infra:prefix",
        "@pypi//pytest",
        "@pypi//pytest_bazel",
        "@pypi//pyyaml",
    ],
)
