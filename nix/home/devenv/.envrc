use flake . --impure

# Auto-start services on directory enter
if [[ ! -f .devenv/processes.pid ]]; then
  echo "Starting services..."
  devenv up --detach 2>/dev/null || echo "Run 'devenv up' manually to start services"
fi

# Teardown happens only when you explicitly run:
# devenv down