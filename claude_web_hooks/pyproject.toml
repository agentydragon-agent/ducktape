[project]
name = "claude-web-hooks"
version = "0.0.0"
description = "Session hooks for Claude Code web environments."
authors = [{ name = "Rai" }]
requires-python = ">=3.12"
# IMPORTANT: No runtime dependencies allowed.
# This package is used by session-start hooks which run before package installation.
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.3",
]
