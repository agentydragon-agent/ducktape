load("@npm_rspcache_admin//:defs.bzl", "npm_link_all_packages")

# Link npm packages from pnpm-lock.yaml (converted from package-lock.json)
npm_link_all_packages(name = "node_modules")

# Export files needed by npm_translate_lock
exports_files([
    "pnpm-lock.yaml",
    "package.json",
])

# TODO: js_run_binary for Vite build not working - artifact prefix conflict with node_modules
# Build with: npm run build (in rspcache/admin_ui directory)
# TypeScript check with: npm run tsc -- --noEmit
