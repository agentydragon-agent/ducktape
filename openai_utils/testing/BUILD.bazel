load("@rules_python//python:defs.bzl", "py_library")

# NOTE: No aggregator target - use specific per-file targets like :fixtures, :strict_mode_models, :openai_mock
# This is the Gazelle-compatible pattern (python_generation_mode = file)

py_library(
    name = "fixtures",
    testonly = True,
    srcs = ["fixtures.py"],
    imports = ["../.."],
    visibility = ["//:__subpackages__"],
    deps = [
        "//openai_utils:client_factory",
        "//openai_utils:model",
        "@pypi//httpx",
        "@pypi//openai",
        "@pypi//pytest",
    ],
)

py_library(
    name = "openai_mock",
    srcs = ["openai_mock.py"],
    imports = ["../.."],
    visibility = ["//:__subpackages__"],
    deps = [
        "//openai_utils:model",
        "@pypi//pydantic",
    ],
)

py_library(
    name = "strict_mode_models",
    srcs = ["strict_mode_models.py"],
    imports = ["../.."],
    visibility = ["//:__subpackages__"],
    deps = ["@pypi//pydantic"],
)
