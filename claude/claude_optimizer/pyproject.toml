[project]
name = "claude_optimizer"
version = "0.1.0"

dependencies = [
    "openai>=1.0.0",
    "anthropic",
    "claude-code-sdk",
    "pydantic>=2.0.0",
    "structlog",
    "plotnine",
    "pandas",
    "numpy",
    "pyyaml",
    "tiktoken",
    "docker",
    "pathspec",
]

[project.optional-dependencies]
dev = [
    # Note: pytest disabled - tests require code from llm/adgn_llm/instruction_optimizer
    # "pytest>=7.0.0",
    # "pytest-asyncio",
    # "pytest-mock",
    # "pytest-cov",
    "black",
    "ruff",
    "mypy",
    "pre-commit",
    # Type stubs
    "types-requests",
    "types-PyYAML",
    "types-setuptools",
    "types-docutils",
    "pandas-stubs",
]
