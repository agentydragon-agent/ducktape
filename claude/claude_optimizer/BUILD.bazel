load("@rules_python//python:defs.bzl", "py_library")

# NOTE: No aggregator target - use specific per-file targets like :generic_graders, :scoresheet
# This is the Gazelle-compatible pattern (python_generation_mode = file)

py_library(
    name = "generic_graders",
    srcs = ["generic_graders.py"],
    visibility = ["//:__subpackages__"],
    deps = [
        ":scoresheet",
        "@pypi//openai",
        "@pypi//pydantic",
    ],
)

py_library(
    name = "scoresheet",
    srcs = ["scoresheet.py"],
    visibility = ["//:__subpackages__"],
)
