# Minimal pyproject.toml - Bazel is the build system
# Tool configs are in dedicated files (ruff.toml, mypy.ini, etc.)

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
