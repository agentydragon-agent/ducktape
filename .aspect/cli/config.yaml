# Aspect CLI configuration
# Docs: https://docs.aspect.build/cli/

lint:
  aspects:
    # Python
    - //tools/lint:linters.bzl%ruff
    - //tools/lint:linters.bzl%mypy_aspect
    # JavaScript/TypeScript
    - //tools/lint:linters.bzl%eslint
