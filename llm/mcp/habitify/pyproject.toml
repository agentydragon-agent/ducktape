[project]
name = "habitify-mcp-server"
version = "0.1.0"
description = "Habitify API MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]",
    "httpx",
    "python-dotenv",
    "typer>=0.9.0",
    "rich>=10.0.0",
    "pydantic>=2.0.0",
]
