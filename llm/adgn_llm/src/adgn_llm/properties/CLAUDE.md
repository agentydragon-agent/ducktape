@README.md

Make sure to:

- Follow out required markdown properties when writing markdown
- Properly split findings between `covered.md` / `not_covered_yet.md`
  - ONLY findings that WOULD be flagged as violations of an ALREADY EXISTING property in `props/**` go into `covered.md`
  - Findings that DO NOT map to existing definition in `props/**` OR map only tangentially go to `not_covered_yet.md`
