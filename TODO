add to installation on small laptop: nmap, other hacking tools

merge agentydragon & gpd dotfiles:
    - rcrc

start Signal minimized
    -> difficult; settings are stored in an encrypted sqlite,
    there might be some tools to decrypt but looks quite annoying
    to do.

nvim-treesitter: folding
    -- https://github.com/nvim-treesitter/nvim-treesitter?tab=readme-ov-file#folding
    vim.wo.foldmethod = 'expr'
    vim.wo.foldexpr = 'v:lua.vim.treesitter.foldexpr()'

TODO: add apt-file? not sure, hmm, looks quite heavy
TODO: get rid of login_event_webhook_reporter -- already got activitywatch. might combine with halinuxcompanion => have it work
TODO: rcm has symlink_dirs -- use
TODO: properly pick a sane license schema probably agpl
