[project]
name = "gnome-terminal-profile-switcher"
version = "0.1.0"
authors = [{name = "Rai"}]
requires-python = ">=3.13"
dependencies = [
  "absl-py>=1.0.0",
  "dbus-python>=1.3.0",
  "PyGObject>=3.40.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]