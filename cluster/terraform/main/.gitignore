# Nebula certs — intermediate artifacts generated by tofu apply.
# Content is preserved in tofu state and embedded into machine configs
# and SealedSecrets. Regenerated from CA on apply.
nebula-certs/

# Tofu working files
.terraform/
*.tfstate
*.tfstate.backup
errored.tfstate

# Generated kubeconfig/talosconfig — contains cluster credentials
kubeconfig
talosconfig.yml
