# Nebula certs — derived from CA (in SOPS) at tofu apply time.
nebula-certs/

# Tofu working files
.terraform/
*.tfstate
*.tfstate.backup
errored.tfstate

# Generated kubeconfig/talosconfig — contains cluster credentials
kubeconfig
talosconfig.yml
