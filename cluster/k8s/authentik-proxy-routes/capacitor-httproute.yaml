# HTTPRoute for Capacitor via shared Authentik proxy outpost.
# Traffic flows: Gateway → outpost (auth) → capacitor backend.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: capacitor
  namespace: authentik
spec:
  parentRefs:
    - name: cluster-gateway
      namespace: gateway-system
  hostnames:
    - "capacitor.allegedly.works"
  rules:
    - backendRefs:
        - name: authentik-server
          port: 80
