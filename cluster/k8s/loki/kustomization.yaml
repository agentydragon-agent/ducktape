apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # External access handled by Authentik proxy outpost (native blueprint in k8s/authentik/sso-blueprints.yaml).
  # Traffic flows: Gateway → ak-outpost-loki-outpost (auth) → loki-stack backend.
  # Grafana accesses Loki internally via cluster service (unaffected).
