apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: google-workspace-mcp
  namespace: flux-system
spec:
  retryInterval: 1m
  interval: 10m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./cluster/k8s/google-workspace-mcp
  prune: true
  wait: true
  dependsOn:
    - name: airlock # google-client-credentials SealedSecret (Reflector mirrors it here)
    - name: proxmox-csi
    - name: reflector
