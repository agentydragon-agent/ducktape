apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-mcp-fastmcp-state
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-hetzner
  resources:
    requests:
      storage: 100Mi
