apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-config
  namespace: grocy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: lvm-proxmox-hdd
  resources:
    requests:
      storage: 5Gi
