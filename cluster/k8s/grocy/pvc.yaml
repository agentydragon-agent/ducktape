apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-config
  namespace: grocy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: TODO # replace before unsuspending
  resources:
    requests:
      storage: 5Gi
