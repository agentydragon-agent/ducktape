apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-hetzner
  resources:
    requests:
      storage: 1Gi
