apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mcp-infra
  namespace: flux-system
spec:
  interval: 10m
  path: ./cluster/k8s/mcp-infra
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: valkey
