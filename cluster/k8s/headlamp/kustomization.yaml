apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml
  # HTTPRoute moved to authentik-proxy-routes/headlamp-httproute.yaml
  # (traffic flows through Authentik proxy outpost for authentication)
