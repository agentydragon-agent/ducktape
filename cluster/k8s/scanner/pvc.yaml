apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scanner-data
  namespace: scanner
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi-retain
  resources:
    requests:
      storage: 20Gi
