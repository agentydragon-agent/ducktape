apiVersion: v1
kind: Service
metadata:
  name: oauth-broker
  namespace: oauth-broker
spec:
  type: ClusterIP
  selector:
    app: oauth-broker
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
