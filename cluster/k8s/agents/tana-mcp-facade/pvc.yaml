apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tana-mcp-facade-fastmcp-state
  namespace: tana-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-proxmox
  resources:
    requests:
      storage: 1Gi
