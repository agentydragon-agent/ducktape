apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kagent

resources:
  - gitrepository.yaml
  - crds-helmrelease.yaml
  - helmrelease.yaml
  - authentik-proxy-service.yaml
  # TODO: When un-suspending, switch to proxy mode outpost and add HTTPRoute
  # (forward_single mode is not supported with Gateway API).
  # See k8s/authentik/app/blueprints/ and k8s/authentik/proxy-routes/ for the proxy mode pattern.
