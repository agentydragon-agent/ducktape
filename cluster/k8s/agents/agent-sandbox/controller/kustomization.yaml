apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # v0.5.5's combined release asset is the upstream GitOps install contract:
  # one collision-free controller plus core and extension CRDs/RBAC. The
  # downloaded asset was verified as SHA-256
  # aaa9d931acb8af90a0a458b6d72bd245d224faac7117e8a241f9e7086acc24e9.
  - https://github.com/kubernetes-sigs/agent-sandbox/releases/download/v0.5.5/sandbox-with-extensions.yaml
patches:
  - path: patches.yaml
