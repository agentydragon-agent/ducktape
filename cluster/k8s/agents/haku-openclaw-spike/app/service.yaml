apiVersion: v1
kind: Service
metadata:
  name: haku-openclaw-spike
  namespace: haku-openclaw-spike
spec:
  selector:
    app.kubernetes.io/name: haku-openclaw-spike
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
