apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tana-mcp-config
  namespace: tana-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi-retain
  resources:
    requests:
      storage: 1Gi
