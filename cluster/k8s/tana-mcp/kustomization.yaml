apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - httproute.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: tana-mcp-nginx-config
    namespace: tana-mcp
    files:
      - default.conf.template=nginx-auth-proxy.conf
