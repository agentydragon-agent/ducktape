apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-storage
  namespace: openclaw-sandbox
spec:
  storageClassName: proxmox-csi-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
