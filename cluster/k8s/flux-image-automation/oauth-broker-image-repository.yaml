apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: oauth-broker
  namespace: flux-system
spec:
  image: registry.allegedly.works/ducktape/oauth-broker
  interval: 5m
  secretRef:
    name: harbor-ci-robot
