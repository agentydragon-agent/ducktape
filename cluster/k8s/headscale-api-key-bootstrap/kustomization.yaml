apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - job.yaml

configMapGenerator:
  - name: headscale-api-key-bootstrap-scripts
    namespace: headscale
    files:
      - setup.sh
