version: 1
metadata:
  name: SSO - Headlamp
  labels:
    blueprints.goauthentik.io/description: "Headlamp OAuth2 OIDC provider and application"

# CLEANUP(2026-04-16): Tombstones — delete this file after Authentik reconciles.
# Provider and application now managed by sso-providers TF module.
entries:
  - model: authentik_providers_oauth2.oauth2provider
    state: absent
    identifiers:
      name: headlamp

  - model: authentik_core.application
    state: absent
    identifiers:
      slug: headlamp
