version: 1
metadata:
  name: SSO - Vault
  labels:
    blueprints.goauthentik.io/description: "Vault OIDC provider and application"

entries:
  # CLEANUP(2026-04-12): Remove once confirmed absent (vault-oidc-auth TF module removed,
  #   Vault OIDC auth backend decommissioned).
  - model: authentik_providers_oauth2.oauth2provider
    state: absent
    identifiers:
      name: vault-oidc

  - model: authentik_core.application
    state: absent
    identifiers:
      slug: vault
