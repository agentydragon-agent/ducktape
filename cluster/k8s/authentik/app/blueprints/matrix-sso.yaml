version: 1
metadata:
  name: SSO - Matrix
  labels:
    blueprints.goauthentik.io/description: "Matrix OIDC provider and application"

entries:
  # CLEANUP(2026-04-18): Remove once confirmed absent — provider+application now managed by
  #   sso-providers TF (cluster/terraform/gitops/sso-providers/main.tf).
  - model: authentik_providers_oauth2.oauth2provider
    state: absent
    identifiers:
      name: matrix-oauth2

  - model: authentik_core.application
    state: absent
    identifiers:
      slug: matrix
