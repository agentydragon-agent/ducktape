# Staging intentionally has no acceptance-only Action fixture. It remains available for ad hoc
# Authentik/Cilium click-through tests; the Flux-managed testing instance owns mcp-everything.
allowed_service_account_namespaces:
  - agentplane-staging
identities:
  personal:
    enabled: true
# No acceptance-only fixture is installed in staging.
