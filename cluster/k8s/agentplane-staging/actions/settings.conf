action_groups:
  everything:
    title: Upstream Everything
    description: Credentialless MCP reference server for staging acceptance.
    executor:
      kind: mcp
      description: Community-built Everything image; no user account, workload token, or mounted credentials.
      config:
        transport: streamable-http
        url: http://agentplane-mcp-everything.agentplane-staging.svc.cluster.local:3001/mcp
fixture_auto_allow:
  group: everything
