# Limits voluntary eviction; it neither prevents node failures nor migrates active TCP streams.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: agentplane-egress
  namespace: agentplane-staging
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agentplane-egress
