apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring-agent-rbac
  namespace: ducktape-flux
  annotations:
    description: Agent RBAC for monitoring namespace. Lightweight — only RoleBindings. Depends on namespace existing + agent RBAC base for ClusterRoles.
spec:
  interval: 10m
  path: ./cluster/k8s/monitoring/agent-rbac
  prune: true
  sourceRef:
    kind: GitRepository
    name: ducktape
    namespace: ducktape-flux
  timeout: 2m
  dependsOn:
    - name: monitoring-namespace
      namespace: ducktape-flux
    - name: claude-rbac
      namespace: ducktape-flux
