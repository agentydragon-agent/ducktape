apiVersion: v1
kind: Service
metadata:
  name: aiquota-api
  namespace: cli-proxy-api
  annotations:
    description: Internal bearer-authenticated API for normalized and raw Claude subscription quota responses. Codex is intentionally deferred.
spec:
  selector:
    app.kubernetes.io/name: aiquota
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
