apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aiquota
  namespace: cli-proxy-api
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: aiquota
  endpoints:
    - port: http
      path: /metrics
      interval: 60s
      scrapeTimeout: 15s
