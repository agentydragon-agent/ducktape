apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - oura-client-credentials-sealed.yaml
  - harbor-robot-eso.yaml
  # google-client-credentials-sealed.yaml — not yet created, re-enable when credentials are available
