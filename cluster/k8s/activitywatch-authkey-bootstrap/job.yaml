---
apiVersion: batch/v1
kind: Job
metadata:
  name: activitywatch-authkey-bootstrap
  namespace: headscale
  annotations:
    kustomize.toolkit.fluxcd.io/prune: "false"
spec:
  ttlSecondsAfterFinished: 86400
  backoffLimit: 10
  template:
    metadata:
      labels:
        app: activitywatch-authkey-bootstrap
    spec:
      restartPolicy: OnFailure
      serviceAccountName: activitywatch-authkey-bootstrap
      containers:
        - name: bootstrap
          image: bitnami/kubectl:latest
          command: ["/bin/bash", "/scripts/setup.sh"]
          volumeMounts:
            - name: scripts
              mountPath: /scripts
              readOnly: true
      volumes:
        - name: scripts
          configMap:
            name: activitywatch-authkey-bootstrap-scripts
            defaultMode: 0755
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: activitywatch-authkey-bootstrap
  namespace: headscale
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: activitywatch-authkey-bootstrap
  namespace: headscale
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "create", "update", "patch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: activitywatch-authkey-bootstrap
  namespace: headscale
subjects:
  - kind: ServiceAccount
    name: activitywatch-authkey-bootstrap
    namespace: headscale
roleRef:
  kind: Role
  name: activitywatch-authkey-bootstrap
  apiGroup: rbac.authorization.k8s.io
