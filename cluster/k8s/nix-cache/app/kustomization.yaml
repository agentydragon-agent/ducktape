apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - jwt-token.sops.yaml
  - bucket.yaml
  - iam.yaml
  - s3-credentials.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

configMapGenerator:
  - name: attic-config
    namespace: nix-cache
    files:
      - server.toml
