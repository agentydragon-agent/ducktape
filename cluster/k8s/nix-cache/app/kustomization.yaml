apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - signing-key.sops.yaml
  - jwt-token.sops.yaml
  - pvcs.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

configMapGenerator:
  - name: attic-config
    namespace: nix-cache
    files:
      - server.toml
