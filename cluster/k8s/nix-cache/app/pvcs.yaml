apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attic-cache
  namespace: nix-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: local-path
