apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: powerdns
  namespace: flux-system
spec:
  interval: 10m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: "./cluster/k8s/powerdns"
  prune: true
  wait: true
  dependsOn:
    - name: dns-system-namespace
    - name: metallb-config # Wait for MetalLB + IPAddressPools
    - name: powerdns-secrets # Wait for password generation
    - name: reflector # Wait for Reflector to handle secret copying
  # Ensure MetalLB can assign VIP and PowerDNS is operational
  healthChecks:
    - apiVersion: metallb.io/v1beta1
      kind: IPAddressPool
      name: dns-pool
      namespace: metallb-system
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: powerdns
      namespace: dns-system
