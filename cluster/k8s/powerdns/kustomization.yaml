apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - daemonset.yaml
  - service.yaml
configMapGenerator:
  - name: powerdns-config
    files:
      - pdns.conf
generatorOptions:
  disableNameSuffixHash: true
