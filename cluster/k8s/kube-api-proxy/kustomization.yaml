apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - certificate.yaml
  - daemonset.yaml
configMapGenerator:
  - name: kube-api-proxy-config
    files:
      - config/nginx.conf
    namespace: kube-api-proxy
