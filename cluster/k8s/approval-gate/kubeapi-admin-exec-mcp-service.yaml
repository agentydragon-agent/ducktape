apiVersion: v1
kind: Service
metadata:
  name: kubeapi-admin-exec-mcp
  namespace: approval-gate
  labels:
    app: approval-gate
    component: kubeapi-admin-exec-mcp
spec:
  selector:
    app: approval-gate
    component: kubeapi-admin-exec-mcp
  ports:
    - name: mcp
      port: 8766
      targetPort: 8766
      protocol: TCP
  type: ClusterIP
