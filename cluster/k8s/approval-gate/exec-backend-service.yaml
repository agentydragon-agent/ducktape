apiVersion: v1
kind: Service
metadata:
  name: exec-backend
  namespace: approval-gate
  labels:
    app: approval-gate
    component: exec-backend
spec:
  selector:
    app: approval-gate
    component: exec-backend
  ports:
    - name: mcp
      port: 8766
      targetPort: 8766
      protocol: TCP
  type: ClusterIP
