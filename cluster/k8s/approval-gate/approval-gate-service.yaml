apiVersion: v1
kind: Service
metadata:
  name: approval-gate
  namespace: approval-gate
  labels:
    app: approval-gate
    component: server
spec:
  selector:
    app: approval-gate
    component: server
  ports:
    - name: http
      port: 8765
      targetPort: 8765
      protocol: TCP
  type: ClusterIP
