apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: tofu-state-db
  namespace: tofu-state
spec:
  parentRefs:
    - name: cluster-gateway
      namespace: gateway-system
      sectionName: postgres-tfstate
  rules:
    - backendRefs:
        - name: tofu-state-db-rw
          port: 5432
