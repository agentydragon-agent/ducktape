apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: haku-ci
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  path: ./cluster/k8s/haku-ci
  prune: true
  # Temporary controller-handoff guard. Remove after this Kustomization
  # has been recreated in ducktape-flux.
  deletionPolicy: Orphan
  # The runner pod stays pending until its registration-token Secret
  # (haku-ci-runner-token) is provisioned during paving — don't block on health.
  wait: false
  sourceRef:
    kind: GitRepository
    name: ducktape

    namespace: ducktape-flux
  dependsOn:
    - name: forgejo # Forgejo (with Actions enabled, #2556) must be up first
    - name: keda # supplies the ScaledObject and TriggerAuthentication CRDs
      namespace: ducktape-flux
    # Reflector mirrors haku-forgejo-tea from the completed managed-agent
    # Kustomization into haku-ci for the native Forgejo KEDA scaler.
    - name: reflector
      namespace: ducktape-flux
    - name: haku-managed-agent
