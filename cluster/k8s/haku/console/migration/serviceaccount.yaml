# The migration process has no Kubernetes API work. Keep it separate from the API
# ServiceAccount, which can manage narrowly scoped Claude SandboxClaims.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: haku-console-migration
  namespace: haku-console
automountServiceAccountToken: false
