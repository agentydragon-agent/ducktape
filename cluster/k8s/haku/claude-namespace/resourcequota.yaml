apiVersion: v1
kind: ResourceQuota
metadata:
  name: haku-claude-sandbox-quota
  namespace: haku-claude-sandbox
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    pods: "10"
    configmaps: "10"
