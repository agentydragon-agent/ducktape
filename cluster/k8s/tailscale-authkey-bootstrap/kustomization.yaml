apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - job.yaml

configMapGenerator:
  - name: tailscale-authkey-bootstrap-scripts
    namespace: headscale
    files:
      - setup.sh
