apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: atuin-user-provisioner
  namespace: flux-system
spec:
  retries: 0
  interval: 10m
  path: ./cluster/k8s/applications/atuin-user-provisioner
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: atuin
    - name: user-agentydragon
