apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - servicemonitor.yaml
  - tlsroute.yaml

configMapGenerator:
  - name: headscale-config
    namespace: headscale
    files:
      - config.yaml
