apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headscale-data
  namespace: headscale
spec:
  accessModes:
    - ReadWriteOnce
  # Use local-path on VPS nodes for low-latency SQLite access
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
