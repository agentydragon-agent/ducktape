apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-tls
  namespace: harbor
spec:
  secretName: harbor-tls
  issuerRef:
    name: "${LETSENCRYPT_ISSUER}"
    kind: ClusterIssuer
  dnsNames:
    - registry.allegedly.works
