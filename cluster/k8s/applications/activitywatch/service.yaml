apiVersion: v1
kind: Service
metadata:
  name: activitywatch
  namespace: activitywatch
spec:
  selector:
    app: activitywatch
  ports:
    - name: http
      port: 5600
      targetPort: 5600
      protocol: TCP
  type: ClusterIP
