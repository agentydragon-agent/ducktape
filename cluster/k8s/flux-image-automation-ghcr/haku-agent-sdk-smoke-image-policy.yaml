apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: haku-agent-sdk-smoke
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: haku-agent-sdk-smoke
  filterTags:
    # Tags pushed by CI: {branch}-YYYYMMDDHHMMSS-{sha7}.
    pattern: '^devel-\d{14}-[0-9a-f]{7}$'
  policy:
    alphabetical:
      order: asc
