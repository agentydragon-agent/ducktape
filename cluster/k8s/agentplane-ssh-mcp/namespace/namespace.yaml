apiVersion: v1
kind: Namespace
metadata:
  name: agentplane-ssh-mcp
  labels:
    name: agentplane-ssh-mcp
  annotations:
    description: Standalone Agentplane SSH MCP backend; private keys stay in this namespace.
