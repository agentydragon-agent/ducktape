[project]
name = "finance"
version = "0.0.0"
description = "Finance reconciliation tools"
requires-python = ">=3.12"
dependencies = [
    "absl-py",
    "fastapi",
    "splitwise",
    "uvicorn",
    "xdg",
]
