apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "minio-ember.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "minio-ember.labels" . | nindent 4 }}
