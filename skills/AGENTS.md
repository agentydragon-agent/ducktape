@README.md

## Skill authoring

Keep `SKILL.md` under 500 lines. Only include context a strong LLM doesn't already have — local specifics, gotchas, bespoke APIs. Use progressive disclosure: put detailed reference material in separate files, not inline.

Frontmatter fields: `name` (must match directory), `description` (what + when to trigger), `allowed-tools`.
