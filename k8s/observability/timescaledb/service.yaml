apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: observability
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: timescaledb
  type: ClusterIP