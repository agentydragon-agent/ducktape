apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

resources:
  - sealed-secret.yaml
  - statefulset.yaml
  - service.yaml

configMapGenerator:
  - name: timescaledb-schema
    files:
      - schema.sql