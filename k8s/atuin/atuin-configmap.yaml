apiVersion: v1
kind: ConfigMap
metadata:
  name: atuin-config
  labels:
    app: atuin
    component: server
data:
  server.toml: |
    # Atuin server configuration
    host = "0.0.0.0"
    port = 8888
    open_registration = false
    
    # Database connection will use environment variable