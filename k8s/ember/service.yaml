apiVersion: v1
kind: Service
metadata:
  name: ember
  namespace: ember
  labels:
    app.kubernetes.io/name: ember
    app.kubernetes.io/component: agent
spec:
  selector:
    app.kubernetes.io/name: ember
    app.kubernetes.io/component: agent
  ports:
    - name: http
      port: 8000
      targetPort: http
