apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-storage
  labels:
    app.kubernetes.io/name: registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

