apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: registry
resources:
  - namespace.yaml
  - pvc.yaml
  - sealedsecret-htpasswd.yaml
  - deployment.yaml
  - service-nodeport.yaml

