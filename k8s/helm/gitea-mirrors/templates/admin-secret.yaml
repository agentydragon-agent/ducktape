apiVersion: v1
kind: Secret
metadata:
  name: gitea-admin-token
  namespace: {{ .Values.namespace | default "gitea" }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "15"
type: Opaque
stringData:
  token: {{ .Values.gitea.admin.token | default "changeme" }}