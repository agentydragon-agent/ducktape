apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "minio-ember.fullname" . }}-policy
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "minio-ember.labels" . | nindent 4 }}
data:
  policy.json: |
{{ tpl (.Files.Get "files/policy.json.tpl") . | indent 4 }}
