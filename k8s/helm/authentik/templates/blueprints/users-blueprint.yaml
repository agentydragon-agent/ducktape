apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints-users
  namespace: authentik
  labels:
{{- include "common.blueprintLabels" . | nindent 4 }}
data:
  users.yaml: {{ .Files.Get "blueprints/users.yaml" | quote }}
