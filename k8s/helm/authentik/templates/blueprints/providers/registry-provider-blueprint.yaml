apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints-registry
  namespace: {{ .Release.Namespace }}
  labels:
{{- include "common.blueprintLabels" . | nindent 4 }}
data:
  registry.yaml: |
{{ tpl (.Files.Get "blueprints/registry.yaml") . | indent 4 }}
