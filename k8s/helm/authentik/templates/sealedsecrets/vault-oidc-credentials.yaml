{{- $annotations := include "common.reflectorAnnotations" (dict "namespaces" "vault") | fromYaml }}
{{ include "common.sealedSecret" (dict
  "context" .
  "namespace" .Release.Namespace
  "name" "vault-oidc-credentials"
  "encryptedData" .Values.secrets.vaultOIDCCredentials
  "annotations" $annotations
) }}
