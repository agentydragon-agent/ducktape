---
# Install and configure headscale

- name: Create headscale group
  ansible.builtin.group:
    name: headscale
    system: true
  tags: [headscale, install]

- name: Create headscale user
  ansible.builtin.user:
    name: headscale
    group: headscale
    home: /var/lib/headscale
    shell: /bin/false
    system: true
    create_home: true
  tags: [headscale, install]

- name: Create headscale config directory
  ansible.builtin.file:
    path: /etc/headscale
    state: directory
    owner: headscale
    group: headscale
    mode: '0755'
  tags: [headscale, install]

- name: Download headscale binary
  ansible.builtin.get_url:
    url: >-
      https://github.com/juanfont/headscale/releases/download/v{{ headscale_server_version
      }}/headscale_{{ headscale_server_version }}_linux_amd64
    dest: /usr/local/bin/headscale
    owner: root
    group: root
    mode: '0755'
  tags: [headscale, install]

- name: Generate noise private key
  ansible.builtin.shell: |
    /usr/local/bin/headscale generate private-key > /var/lib/headscale/noise_private.key
    chown headscale:headscale /var/lib/headscale/noise_private.key
    chmod 600 /var/lib/headscale/noise_private.key
  args:
    creates: /var/lib/headscale/noise_private.key
  tags: [headscale, config]

- name: Generate headscale configuration
  ansible.builtin.template:
    src: config.yaml.j2
    dest: /etc/headscale/config.yaml
    owner: headscale
    group: headscale
    mode: '0644'
  notify: restart headscale
  tags: [headscale, config]

- name: Create headscale systemd service
  ansible.builtin.template:
    src: headscale.service.j2
    dest: /etc/systemd/system/headscale.service
    owner: root
    group: root
    mode: '0644'
  notify:
    - reload systemd
    - restart headscale
  tags: [headscale, service]

- name: Enable and start headscale service
  ansible.builtin.systemd:
    name: headscale
    enabled: true
    state: started
    daemon_reload: true
  tags: [headscale, service]

- name: Check headscale service status
  ansible.builtin.systemd:
    name: headscale
  register: headscale_status
  tags: [headscale, service]

- name: Display headscale service status
  ansible.builtin.debug:
    var: headscale_status.status
  tags: [headscale, service]

- name: Wait for headscale to be ready
  ansible.builtin.uri:
    url: "https://127.0.0.1:8080/health"
    method: GET
    status_code: 200
    timeout: 5
    validate_certs: false
  register: headscale_health
  until: headscale_health.status is defined and headscale_health.status == 200
  retries: 10
  delay: 3
  failed_when: false
  tags: [headscale, service]

- name: Verify headscale readiness
  ansible.builtin.assert:
    that:
      - headscale_health.status is defined
      - headscale_health.status == 200
    fail_msg: >-
      Headscale did not return HTTP 200 after the configured retries.
      Last response: {{ headscale_health }}
  tags: [headscale, service]
