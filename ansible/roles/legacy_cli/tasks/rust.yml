---
# Rust installation for non-Nix systems
# Nix systems use rustc and cargo packages instead

# Install Rust development tools
- name: Check if cargo is installed
  ansible.builtin.command: which cargo
  register: legacy_cli_cargo_check
  failed_when: false
  changed_when: false

- name: Install Rust via rustup
  ansible.builtin.shell: |
    set -o pipefail
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  args:
    creates: "~/.cargo/bin/cargo"
  when: legacy_cli_cargo_check.rc != 0

- name: Ensure cargo bin directory exists
  ansible.builtin.file:
    path: "~/.cargo/bin"
    state: directory
    mode: '0755'
