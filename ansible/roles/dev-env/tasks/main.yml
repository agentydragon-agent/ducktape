---
# Development environment setup is now handled by the cli role's dev-env tasks
# This role serves as a placeholder for backwards compatibility
# TODO: Consider removing this role and updating playbooks to use cli role directly
