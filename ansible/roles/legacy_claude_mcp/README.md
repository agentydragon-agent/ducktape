# Legacy Claude MCP Server List

Reference list of MCP servers that were previously managed by this Ansible role.
The role itself is removed; this file preserves the server list for reference.

| Server    | Command | Package                                                      |
| --------- | ------- | ------------------------------------------------------------ |
| memory    | `npx`   | `@modelcontextprotocol/server-memory`                        |
| firecrawl | `npx`   | `firecrawl-mcp` (env: `FIRECRAWL_API_URL=http://localhost:3002`) |
| arxiv     | `uvx`   | `git+https://github.com/blazickjp/arxiv-mcp-server.git`     |
| probe     | `npx`   | `@buger/probe-mcp`                                          |
