---
- name: Check installed version file for {{ item }}
  ansible.builtin.stat:
    path: "{{ installed_version_path }}"
  register: legacy_nerd_fonts_version_file
  changed_when: false

- name: Read installed version file for {{ item }}
  ansible.builtin.slurp:
    src: "{{ installed_version_path }}"
  register: legacy_nerd_fonts_installed_version
  when: legacy_nerd_fonts_version_file.stat.exists
  changed_when: false

- name: Decide whether to reinstall {{ item }}
  ansible.builtin.set_fact:
    # b64decode: slurp reads content base64-encoded
    legacy_nerd_fonts_need_install: >-
      {{ (
        not legacy_nerd_fonts_version_file.stat.exists
        or (legacy_nerd_fonts_installed_version.content | b64decode | trim != legacy_nerd_fonts_version.stdout | trim)
      ) }}
  changed_when: false

- name: Debug info about reinstall need
  ansible.builtin.debug:
    msg: "{{ item }}: reinstall needed? -> {{ legacy_nerd_fonts_need_install }}"
    verbosity: 1

- name: Download & install {{ item }}
  when: legacy_nerd_fonts_need_install
  block:
    - name: Ensure dir exists for {{ item }}
      ansible.builtin.file:
        path: "{{ family_path }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Get download URL for {{ item }}
      ansible.builtin.shell: |
        set -o pipefail
        jq --raw-output '.assets[]
          | select(.name == "{{ item }}.tar.xz")
          | .browser_download_url' {{ legacy_nerd_fonts_latest_release_json_path }}
      register: legacy_nerd_fonts_font_download_url
      changed_when: false
      args:
        executable: /bin/bash

    - name: Assert URL was found for {{ item }}
      ansible.builtin.assert:
        that:
          - legacy_nerd_fonts_font_download_url.stdout != ""

    - name: Download and extract font archive for {{ item }}
      ansible.builtin.unarchive:
        remote_src: true
        src: "{{ legacy_nerd_fonts_font_download_url.stdout }}"
        dest: "{{ family_path }}"
        mode: "0644"
      register: legacy_nerd_fonts_unarchived_font

      notify:
        - update font version file
        - mark fonts updated
