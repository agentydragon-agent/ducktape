---
# Default trusted users for Nix
# Can be overridden per host or in playbook
nix_trusted_users: "{{ my_user | default(ansible_user_id) }}"

# Experimental features to enable
nix_experimental_features: "nix-command flakes"

# Installation mode
nix_install_mode: "multi-user" # or "single-user"
