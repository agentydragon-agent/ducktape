---
# Heavy/optional development packages that may not be needed on all systems
- name: Install heavy dev packages
  become: true
  ansible.builtin.apt:
    pkg:
      - postgresql
      - postgresql-client
      - android-tools-adb
      - esptool  # ESP8266/ESP32 flashing tool
  when: include_heavy_dev_packages | default(true)
