## aiquota TODOs

in aiquota gnome plugin top bar:
if a provider is at 100% (and not using extra usage) -> show red reset arrow + time to reset
(it means i'm out of usage for the week and can't use it until reset)

2026-07-04: again this would be nice when running out of the restrictive z.ai quota
